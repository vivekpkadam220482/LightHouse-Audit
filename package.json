{
  "name": "lighthouse-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for running Lighthouse audits on URLs from CSV file",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "install-browsers": "playwright install chrome",
    "lighthouse": "node lighthouse-audit.js"
  },
  "keywords": ["playwright", "lighthouse", "e2e", "testing", "performance"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0"
  },
  "dependencies": {
    "csv-parser": "^3.0.0",
    "lighthouse": "^11.4.0",
    "chrome-launcher": "^1.1.0",
    "fs-extra": "^11.1.1"
  }
} 